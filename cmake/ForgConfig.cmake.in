@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if(@FORG_USE_FREETYPE@)
    find_dependency(freetype CONFIG)
endif()

if(@FORG_USE_ZLIB@)
    find_dependency(ZLIB)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/ForgTargets.cmake")

check_required_components(Forg)
