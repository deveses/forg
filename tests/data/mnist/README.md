# MNIST Test Data

Full MNIST data is intentionally not committed. The neural-network tests use
the tiny synthetic IDX image and label files in this directory so loader
behavior can be verified without adding large binary fixtures to the
repository.
