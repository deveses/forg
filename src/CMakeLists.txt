add_subdirectory(forg)
if(APPLE)
    add_subdirectory(swrenderer)
    add_subdirectory(metalrenderer)
    add_subdirectory(macapp)
elseif(WIN32)
    add_subdirectory(swrenderer)
    add_subdirectory(glrenderer)
    add_subdirectory(winapp)
endif()
